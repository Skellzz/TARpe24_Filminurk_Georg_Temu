@model Filminurk.Models.Movies.ImageViewModel

@{
	var photoPhats = "~/multipleFIleUpload/" + (Model.FilePath ?? "noimage.png");
}

<div class="form-group row col-sm-4 offset-2">
	<div>
		@Model.FilePath
	</div>
	<img class="card-image-top" src="@photoPhats" asp-append-version="true"/>
</div>